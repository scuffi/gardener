{
  "name": "gardener",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.25.0",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "build:deploy": "pnpm --filter @gardener/app build && pnpm --dir apps/gardener exec wrangler deploy --dry-run --outdir ../../dist/deploy --containers-rollout none && pnpm --dir apps/github-gateway exec wrangler deploy --dry-run --outdir ../../dist/github-gateway-deploy",
    "check:gateway-plan": "GARDENER_CONFIG_HOME=.tmp/gardener-plan pnpm gardener -- gateway plan --workspace qual-ci-dry-run",
    "check": "pnpm versions:check && pnpm ui:check && pnpm typecheck && pnpm test && pnpm build && pnpm build:deploy && pnpm check:gateway-plan",
    "ui:check": "node scripts/check-ui-conventions.mjs",
    "dev": "pnpm --filter @gardener/app dev",
    "dev:gateway": "pnpm --filter @gardener/github-gateway dev",
    "deploy": "pnpm --filter @gardener/app deploy",
    "deploy:gateway": "pnpm --filter @gardener/github-gateway deploy",
    "gardener": "pnpm --filter @gardener/cli build && node packages/cli/dist/cli.js",
    "preflight:flue-native": "node scripts/flue-native-cutover-preflight.mjs",
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "pnpm -r --if-present test",
    "versions:check": "node scripts/check-versions.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260916.1",
    "typescript": "7.0.2",
    "vitest": "5.0.1",
    "wrangler": "4.131.0"
  }
}
