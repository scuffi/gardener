{
  "name": "gardener",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.25.0",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "build:deploy": "pnpm --filter @gardener/app build && pnpm --dir apps/gardener exec wrangler deploy --dry-run --outdir ../../dist/deploy --containers-rollout none",
    "check": "pnpm versions:check && pnpm check:starter-agents && pnpm ui:check && pnpm typecheck && pnpm test && pnpm build && pnpm check:actions-runtime && pnpm check:cli-package && pnpm check:bridge-dist && pnpm build:deploy",
    "check:bridge-dist": "node scripts/check-bridge-dist.mjs",
    "check:actions-runtime": "node scripts/check-actions-runtime.mjs",
    "check:cli-package": "rm -rf .tmp/cli-pack && mkdir -p .tmp/cli-pack && pnpm --filter gardener-cli-demo pack --pack-destination .tmp/cli-pack && node scripts/check-cli-package.mjs .tmp/cli-pack",
    "ui:check": "node scripts/check-ui-conventions.mjs",
    "dev": "pnpm --filter @gardener/app dev",
    "deploy": "pnpm --filter @gardener/app deploy",
    "gardener": "pnpm --filter gardener-cli-demo build && node packages/cli/dist/cli.js",
    "generate:starter-agents": "tsx scripts/generate-starter-agents.ts",
    "check:starter-agents": "tsx scripts/generate-starter-agents.ts --check",
    "preflight:flue-native": "node scripts/flue-native-cutover-preflight.mjs",
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "pnpm -r --if-present test",
    "versions:check": "node scripts/check-versions.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260920.1",
    "tsx": "4.23.15",
    "typescript": "7.0.2",
    "vitest": "5.0.1",
    "wrangler": "4.135.0"
  }
}
